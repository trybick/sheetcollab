query FilterSheets($searchString: String!) {
  filterSheets(searchString: $searchString) {
    artist
    createdAt
    id
    notes
    title
    year

    users {
      email
      id
      username
    }
  }
}
